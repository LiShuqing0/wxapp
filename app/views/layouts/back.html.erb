<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <title>测试</title>
    <%= stylesheet_link_tag "back" %>
    <%= javascript_include_tag "back" %>
    
    <%= csrf_meta_tag %>
  </head>
<body>
  <section id="container" >
    <%= render "/back/shared/head" %>
    <%= render "/back/shared/nav" %>
    <%= yield %>
    <%= render "/back/shared/foot" %>
  </section>
  <% if flash[:notice] || flash[:alert] %>
    <script type="text/javascript">
      var parent_object = window.parent || window;
      <% if flash[:notice] %>
        parent_object.showTip("success","<%= flash[:notice] %>");
      <% elsif flash[:alert] %>
        parent_object.showTip("warning","<%= flash[:alert] %>");
      <% end %>
      <% flash.clear %>
    </script>
  <% end %>
</body>
</html>
